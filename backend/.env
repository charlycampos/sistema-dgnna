# ── Copiar este archivo como .env y ajustar los valores ──────────

# Clave secreta JWT (cambiar en producción)
SESSION_SECRET=dgnna-sistema-dgnna-secret-2026

# ── Base de datos ─────────────────────────────────────────────────

# SQLite local (comentado, se usa como fallback)
# DATABASE_URL=sqlite:///./apelaciones.db

# Oracle XE local
DATABASE_URL=oracle+oracledb://system:123456@localhost:1521/?service_name=XEPDB1
